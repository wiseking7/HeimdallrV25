<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:ctrls="clr-namespace:Heimdallr.UI.Controls">

  <!--#region 1️ GridViewColumnHeaderStyle 정의-->
  <Style x:Key="GridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
    <Setter Property="Background" Value="{Binding Background, RelativeSource={RelativeSource AncestorType=Border}}" />
    <!--  헤더 배경색  -->
    <Setter Property="Height" Value="{Binding Height, RelativeSource={RelativeSource AncestorType=GridViewHeaderRowPresenter}}" />
    <!--  헤더 높이  -->
    <Setter Property="FontSize" Value="{Binding ColumnHeaderFontSize, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
    <!--  글자 크기  -->
    <Setter Property="FontWeight" Value="{Binding ColumnHeaderFontWeight, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
    <!--  글자 두께  -->
    <Setter Property="Foreground" Value="{Binding ColumnHeaderForeground, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
    <!--  글자 색상  -->
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <!--  정렬  -->
    <Setter Property="Padding" Value="5" />
    <!--  패딩  -->

    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="GridViewColumnHeader">
          <Border
            x:Name="HeaderBorder"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True">
            <ContentPresenter
              Margin="{TemplateBinding Padding}"
              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
              VerticalAlignment="Center" />
          </Border>

          <ControlTemplate.Triggers>
            <!--  마우스 오버  -->
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="HeaderBorder" Property="Background" Value="{Binding ColumnHeaderHoverBackground, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
              <Setter TargetName="HeaderBorder" Property="BorderBrush" Value="{Binding ColumnHeaderHoverBorderBrush, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
            </Trigger>

            <!--  마우스 클릭 / Pressed  -->
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="HeaderBorder" Property="Background" Value="{Binding ColumnHeaderPressedBackground, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
              <Setter TargetName="HeaderBorder" Property="BorderBrush" Value="{Binding ColumnHeaderPressedBorderBrush, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <!--#endregion-->

  <!--#region 2 ListViewItem 스타일-->
  <Style TargetType="ListViewItem">
    <Setter Property="Background" Value="{Binding ItemBackground, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
    <Setter Property="Foreground" Value="{Binding ItemForeground, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
    <Setter Property="Height" Value="{Binding ItemRowHeight, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListViewItem">
          <Border
            x:Name="Bd"
            Background="{TemplateBinding Background}"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True">

            <!--  RowPresenter 대신 ItemsPanel에서 각 Cell Template 사용  -->
            <GridViewRowPresenter
              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
              Columns="{Binding View.Columns, RelativeSource={RelativeSource AncestorType=ListView}}"
              Content="{TemplateBinding Content}" />
          </Border>

          <ControlTemplate.Triggers>
            <!--  마우스 오버  -->
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{Binding ItemHoverBackground, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
            </Trigger>

            <!--  선택  -->
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{Binding ItemSelectedBackground, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
            </Trigger>

            <!--  선택 + 마우스 오버  -->
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsSelected" Value="True" />
                <Condition Property="IsMouseOver" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Property="Background" Value="{Binding ItemSelectedHoverBackground, RelativeSource={RelativeSource AncestorType=ctrls:HeimdallrListView}}" />
            </MultiTrigger>

          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <!--#endregion-->

  <!--#region 3 HeimdallrListView 통합 스타일-->
  <Style TargetType="{x:Type ctrls:HeimdallrListView}">
    <!--  기본 배경 / 테두리 / 글자색  -->
    <Setter Property="Background" Value="#FF2E323A" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Foreground" Value="#FFFFFFFF" />

    <!--  아이템 높이  -->
    <Setter Property="ItemContainerStyle" Value="{StaticResource {x:Type ListViewItem}}" />
    <Setter Property="ItemBackground" Value="#FF2E323A" />
    <Setter Property="ItemForeground" Value="#FFEAEAEA" />
    <Setter Property="ItemHoverBackground" Value="#FF3B3F4A" />
    <Setter Property="ItemSelectedBackground" Value="#FF505561" />
    <Setter Property="ItemSelectedHoverBackground" Value="#FF606880" />
    <Setter Property="ItemRowHeight" Value="35" />

    <!--  컬럼 헤더 기본 속성  -->
    <Setter Property="ColumnHeaderFontSize" Value="16" />
    <Setter Property="ColumnHeaderHeight" Value="40" />
    <Setter Property="ColumnHeaderBackground" Value="#FF393D46" />
    <Setter Property="ColumnHeaderForeground" Value="#FFFFFFFF" />
    <Setter Property="ColumnHeaderFontWeight" Value="ExtraLight" />

    <Setter Property="ColumnHeaderHoverBackground" Value="#FF505561" />
    <Setter Property="ColumnHeaderHoverBorderBrush" Value="Transparent" />
    <Setter Property="ColumnHeaderPressedBackground" Value="#FF3F4451" />
    <Setter Property="ColumnHeaderPressedBorderBrush" Value="Transparent" />

    <!--  선택 모드  -->
    <Setter Property="SelectionMode" Value="Single" />

    <!--  Virtualization  -->
    <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
    <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
    <Setter Property="ScrollViewer.CanContentScroll" Value="True" />

    <!--  ItemsPanel  -->
    <Setter Property="ItemsPanel">
      <Setter.Value>
        <ItemsPanelTemplate>
          <VirtualizingStackPanel />
        </ItemsPanelTemplate>
      </Setter.Value>
    </Setter>

    <!--  GridView 설정  -->
    <Setter Property="View">
      <Setter.Value>
        <GridView ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}" />
      </Setter.Value>
    </Setter>

    <!--  ControlTemplate  -->
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ctrls:HeimdallrListView}">
          <Border
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True">

            <ctrls:HeimdallrScrollViewer
              Background="Transparent"
              Focusable="False"
              HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
              VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">

              <DockPanel>
                <!--  헤더 (고정)  -->
                <Border
                  x:Name="HeaderBackgroundBorder"
                  Height="{TemplateBinding ColumnHeaderHeight}"
                  Background="{TemplateBinding ColumnHeaderBackground}"
                  DockPanel.Dock="Top"
                  SnapsToDevicePixels="True"
                  UseLayoutRounding="True">

                  <GridViewHeaderRowPresenter ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}" Columns="{Binding View.Columns, RelativeSource={RelativeSource TemplatedParent}}" />
                </Border>

                <!--  아이템 영역  -->
                <ItemsPresenter />

              </DockPanel>
            </ctrls:HeimdallrScrollViewer>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <!--#endregion-->
</ResourceDictionary>
