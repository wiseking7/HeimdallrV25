<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctrls="clr-namespace:Heimdallr.UI.Controls">

  <Style TargetType="{x:Type ctrls:HeimdallrMaximizeButton }" >
    <Setter Property="Margin" Value="2" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ctrls:HeimdallrMaximizeButton}">
          <Border Background="{TemplateBinding Background}"
                Width="{TemplateBinding IconSize}"
                Height="{TemplateBinding IconSize}" >
            <Viewbox Stretch="{TemplateBinding Stretch}"
                   StretchDirection="Both">
              <ctrls:HeimdallrIcon x:Name="PART_IMG"
                                   Fill="{TemplateBinding Fill}"
                                   Icon="Maximize" >
              </ctrls:HeimdallrIcon>
            </Viewbox>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="PART_IMG" Property="Fill" Value="#FFD8D8D8" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="PART_IMG" Property="Fill" Value="#FFE5B8F4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
