<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:cnvts="clr-namespace:Heimdallr.UI.Converters"
  xmlns:ctrls="clr-namespace:Heimdallr.UI.Controls">

  <cnvts:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

  <BorderGapMaskConverter x:Key="BorderGapMaskConverter" />

  <Style TargetType="{x:Type ctrls:HeimdallrGroupBox}">
    <Setter Property="BorderBrush" Value="#FF456882" />
    <Setter Property="Foreground" Value="#FFFFFFFF" />
    <Setter Property="FontWeight" Value="ExtraLight" />
    <Setter Property="HeaderForeground" Value="#FF7AAACE" />
    <Setter Property="HeaderFontSize" Value="18" />
    <Setter Property="HeaderFontWeight" Value="ExtraLight" />
    <Setter Property="FontSize" Value="18" />
    <Setter Property="IsCollapsible" Value="False" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ctrls:HeimdallrGroupBox}">
          <Grid SnapsToDevicePixels="true">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="6" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="6" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
              <RowDefinition Height="6" />
            </Grid.RowDefinitions>

            <Border
              Grid.Row="1"
              Grid.RowSpan="3"
              Grid.Column="0"
              Grid.ColumnSpan="4"
              Background="{TemplateBinding Background}"
              BorderBrush="Transparent"
              BorderThickness="{TemplateBinding BorderThickness}"
              CornerRadius="{TemplateBinding CornerRadius}"
              SnapsToDevicePixels="True" />

            <Border
              Grid.Row="1"
              Grid.RowSpan="3"
              Grid.ColumnSpan="4"
              BorderBrush="{TemplateBinding BorderBrush}"
              BorderThickness="{TemplateBinding BorderThickness}"
              CornerRadius="{TemplateBinding CornerRadius}"
              SnapsToDevicePixels="True">

              <Border.OpacityMask>
                <MultiBinding Converter="{StaticResource BorderGapMaskConverter}" ConverterParameter="20">
                  <Binding ElementName="Header" Path="ActualWidth" />
                  <Binding Path="ActualWidth" RelativeSource="{RelativeSource Mode=Self}" />
                  <Binding Path="ActualHeight" RelativeSource="{RelativeSource Mode=Self}" />
                </MultiBinding>
              </Border.OpacityMask>

              <Border
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="{TemplateBinding CornerRadius}"
                SnapsToDevicePixels="True">
                <!--<Border BorderBrush="White" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2"/>-->
              </Border>
            </Border>

            <Border
              x:Name="Header"
              Grid.Row="0"
              Grid.RowSpan="2"
              Grid.Column="1"
              Padding="19,1,-7,0"
              SnapsToDevicePixels="True">

              <Grid x:Name="HeaderGrid">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  아이콘  -->
                <ctrls:HeimdallrIcon
                  x:Name="PART_HeaderIcon"
                  Grid.Column="0"
                  Margin="2,0,6,0"
                  Fill="{Binding IconFill, RelativeSource={RelativeSource TemplatedParent}}"
                  Icon="{Binding Icon, RelativeSource={RelativeSource TemplatedParent}}"
                  IconSize="{Binding IconSize, RelativeSource={RelativeSource TemplatedParent}}" />

                <!--  Header 텍스트  -->
                <ContentPresenter
                  x:Name="ContentArea"
                  Grid.Column="1"
                  ContentSource="Header"
                  RecognizesAccessKey="True"
                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                  TextBlock.FontSize="{Binding HeaderFontSize, RelativeSource={RelativeSource TemplatedParent}}"
                  TextBlock.FontWeight="{Binding HeaderFontWeight, RelativeSource={RelativeSource TemplatedParent}}"
                  TextBlock.Foreground="{Binding HeaderForeground, RelativeSource={RelativeSource TemplatedParent}}" />
              </Grid>
            </Border>

            <!--  Content Area  -->
            <ContentPresenter
              x:Name="PART_ContentArea"
              Grid.Row="2"
              Grid.Column="1"
              Grid.ColumnSpan="2"
              Margin="{TemplateBinding Padding}"
              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
          </Grid>

          <!--  트리거 정의  -->
          <ControlTemplate.Triggers>
            <!--  접힘 상태  -->
            <Trigger Property="IsExpanded" Value="False">
              <Setter TargetName="PART_ContentArea" Property="Visibility" Value="Collapsed" />
              <Setter Property="MaxHeight" Value="40" />
              <Setter Property="MinHeight" Value="40" />
              <!--  접혔을 때 기본 높이  -->
            </Trigger>
            <!--  펼침 상태  -->
            <Trigger Property="IsExpanded" Value="True">
              <Setter TargetName="PART_ContentArea" Property="Visibility" Value="Visible" />
              <Setter Property="MinHeight" Value="0" />
              <!--  펼쳤을 때는 자식 크기에 맞게  -->
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>



