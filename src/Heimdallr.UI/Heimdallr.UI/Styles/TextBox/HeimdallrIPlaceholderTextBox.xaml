<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:ctrls="clr-namespace:Heimdallr.UI.Controls">

  <Style TargetType="{x:Type ctrls:HeimdallrPlaceholderTextBox}">
    <!--  기본 속성  -->
    <Setter Property="Padding" Value="5" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="#FF808080" />
    <Setter Property="CornerRadius" Value="7" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Width" Value="200" />
    <Setter Property="Padding" Value="2" />
    <Setter Property="CaretBrush" Value="#FFFFFFFF" />
    <Setter Property="Height" Value="35" />
    <Setter Property="Foreground" Value="#FFDFD0B8" />
    <Setter Property="FontSize" Value="16" />
    <Setter Property="Cursor" Value="IBeam" />
    <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />

    <!--  커스텀 속성  -->
    <Setter Property="IconFill" Value="#FF1597BB" />
    <Setter Property="IconSize" Value="26" />
    <Setter Property="RightIconSize" Value="20" />
    <Setter Property="RightIconFill" Value="#FFA0C1B8" />
    <Setter Property="MouseOverBorderBrush" Value="#FF696969" />
    <Setter Property="PlaceholderForeground" Value="#FFDCD7C9" />
    <Setter Property="FocusedBorderBrush" Value="#FF818FB4" />
    <Setter Property="IsFloatingLabel" Value="True" />

    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ctrls:HeimdallrPlaceholderTextBox}">
          <Border
            x:Name="border"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            CornerRadius="{TemplateBinding CornerRadius}"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True">

            <Grid Margin="{TemplateBinding Padding}">
              <Grid.ColumnDefinitions>
                <!--  이이콘 자리  -->
                <ColumnDefinition Width="Auto" />

                <!--  문자열 자리  -->
                <ColumnDefinition Width="*" />

                <!--  우측버튼  -->
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <!--  좌측 아이콘  -->
              <Viewbox
                Grid.Column="0"
                Width="{TemplateBinding IconSize}"
                Height="{TemplateBinding IconSize}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Stretch="Uniform"
                StretchDirection="Both">
                <ctrls:HeimdallrIcon
                  Padding="2"
                  Fill="{TemplateBinding IconFill}"
                  Icon="{TemplateBinding Icon}"
                  IconMouseOverFill="{TemplateBinding IconMouseOverFill}" />
              </Viewbox>

              <!--  TextBox ContentHost  -->
              <ScrollViewer
                x:Name="PART_ContentHost"
                Grid.Column="1"
                Margin="2,0,0,0"
                VerticalAlignment="Center"
                Background="Transparent"
                Focusable="True"
                HorizontalScrollBarVisibility="Hidden"
                VerticalScrollBarVisibility="Hidden" />

              <!--  Placeholder  -->
              <TextBlock
                x:Name="PlaceholderTextBlock"
                Grid.Column="1"
                Margin="5,0,0,0"
                VerticalAlignment="Center"
                Panel.ZIndex="1"
                Foreground="{TemplateBinding PlaceholderForeground}"
                IsHitTestVisible="False"
                Text="{TemplateBinding PlaceholderText}"
                TextTrimming="CharacterEllipsis"
                Visibility="Collapsed">

                <TextBlock.RenderTransform>
                  <TranslateTransform />
                </TextBlock.RenderTransform>
              </TextBlock>

              <!--  우측 아이콘 버튼  -->
              <Button
                x:Name="PART_RightButton"
                Grid.Column="2"
                Width="{TemplateBinding IconSize}"
                Height="{TemplateBinding IconSize}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="Transparent"
                BorderThickness="0"
                Command="{TemplateBinding RightCommand}">

                <Button.Style>
                  <Style TargetType="Button">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="OverridesDefaultStyle" Value="True" />
                    <Setter Property="Template">
                      <Setter.Value>
                        <ControlTemplate TargetType="Button">
                          <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </ControlTemplate>
                      </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="Transparent" />
                      </Trigger>
                      <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{Binding FocusedBorderBrush, RelativeSource={RelativeSource TemplatedParent}}" />
                      </Trigger>
                    </Style.Triggers>
                  </Style>
                </Button.Style>

                <Viewbox
                  Width="{TemplateBinding RightIconSize}"
                  Height="{TemplateBinding RightIconSize}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Stretch="Uniform"
                  StretchDirection="Both">
                  <ctrls:HeimdallrIcon
                    Padding="2"
                    Fill="{TemplateBinding RightIconFill}"
                    Icon="{TemplateBinding RightIcon}"
                    ToolTip="{TemplateBinding RightIconToolTip}" />
                </Viewbox>
              </Button>
            </Grid>
          </Border>

          <ControlTemplate.Triggers>

            <!--  오류 시  -->
            <Trigger Property="Validation.HasError" Value="True">
              <Setter TargetName="border" Property="BorderBrush" Value="#FFB00020" />
            </Trigger>

            <!--#region 기본 Placeholder-->
            <Trigger Property="Text" Value="">
              <Setter TargetName="PlaceholderTextBlock" Property="Visibility" Value="Visible" />
            </Trigger>
            <!--#endregion-->

            <!--#region Floating Label ON-->
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsFloatingLabel" Value="True" />
                <Condition Property="IsKeyboardFocusWithin" Value="True" />
              </MultiTrigger.Conditions>

              <Setter TargetName="PlaceholderTextBlock" Property="Visibility" Value="Visible" />
              <Setter TargetName="PlaceholderTextBlock" Property="RenderTransform">
                <Setter.Value>
                  <TranslateTransform X="10" Y="-25" />
                </Setter.Value>
              </Setter>
              <Setter TargetName="PlaceholderTextBlock" Property="FontSize" Value="11" />
            </MultiTrigger>
            <!--#endregion-->

            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="border" Property="BorderBrush" Value="{Binding MouseOverBorderBrush, RelativeSource={RelativeSource TemplatedParent}}" />
            </Trigger>

            <Trigger Property="IsKeyboardFocusWithin" Value="True">
              <Setter TargetName="border" Property="BorderBrush" Value="{Binding FocusedBorderBrush, RelativeSource={RelativeSource TemplatedParent}}" />
              <Setter TargetName="border" Property="BorderThickness" Value="0.5,0.5,0.5,3" />
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="border" Property="Opacity" Value="0.6" />
              <Setter Property="Cursor" Value="Arrow" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
